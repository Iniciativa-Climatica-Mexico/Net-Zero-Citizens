<resources>
    <!-- Base application theme. -->
    <style name="Theme.GreenCircle" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/primary_900</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/green</item>
        <item name="colorSecondaryVariant">@color/secondary_900</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="android:textColorLink">@color/primary_900</item>
    </style>

    <style name="Theme.GreenCircle.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.GreenCircle.CustomRadioButtonStyle">
        <item name="android:background">@drawable/custom_radio_selector</item>
        <item name="android:textColor">@drawable/custom_radio_text_selector</item>
        <item name="android:button">@android:color/transparent</item>
        <item name="android:textAlignment">center</item>
    </style>

</resources>